## Installation of complete app

Assuming you have [Node](https://nodejs.org/en/) installed, clone this repo and run `npm install` and `npm start` in the cloned directory.

#### Command line:

```
npm install

npm start
```
